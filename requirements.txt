# ---- Machine learning / NLP (free & open-source, runs 100% locally) ----
torch>=2.5.0
transformers>=4.48.0
sentence-transformers>=3.0.0

# ---- LangChain ecosystem (orchestration) ----
langchain>=1.0.0
langchain-community>=0.4.0
langchain-huggingface>=1.2.0
langchain-text-splitters>=1.1.0
faiss-cpu>=1.7.0

# ---- Document parsing ----
pypdf>=4.0.0
docx2txt>=0.9
reportlab>=4.0.0

# ---- Backend & UI ----
fastapi>=0.110.0
uvicorn>=0.30.0
streamlit>=1.40.0
pandas>=2.0.0

# ---- Database ----
psycopg2-binary>=2.9.0  # PostgreSQL adapter (production)

# ---- SSO / OIDC ----
httpx>=0.27.0            # HTTP client for OIDC discovery + token exchange

# ---- MFA (TOTP) ----
pyotp>=2.9.0             # TOTP generation + verification
qrcode[pil]>=7.0         # QR code generation for authenticator apps

# ---- Encryption at rest ----
cryptography>=43.0.0     # Fernet symmetric encryption

# ---- LDAP (optional) ----
# ldap3>=2.9.1           # Uncomment for LDAP SSO support

# ---- Testing ----
pytest>=9.0.0
